node最牛逼的特性：事件驱动和异步事件~支持起大并发请求~~~

json的特性：
- json是纯文本，所有字符串必须双引号括起

CRUD操作：create/read/update/delete

use database
db.dropDatabase

db.collection.drop()
db.collection.insert()
db.collection.find()
db.collection.update()
db.collection.remove()

$set
$unset
$lt
$gt
$regex

阅读至，77页